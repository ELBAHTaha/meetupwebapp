# Point the frontend at the live hudlgo backend. Leave blank to use the built-in
# in-memory mock (fully offline demo).
VITE_API_URL=http://localhost:4000

# Paddle — client-side token (safe to expose in frontend) + environment.
# Leave the token blank to use the dev simulation (no Paddle overlay shown).
VITE_PADDLE_CLIENT_TOKEN=
VITE_PADDLE_ENV=sandbox
